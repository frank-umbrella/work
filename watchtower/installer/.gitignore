build/
dist/
*.spec

# Vendor MSIs (LogMeIn, etc.) — kept locally for build.ps1 -LogmeinMsi but
# never tracked. Each MSP has their own copy; bytes don't belong in the repo.
vendor/
*.msi

# Generated by build.ps1 from branding/source/Logo-House-Icon.png on first
# run. Regenerable; not worth tracking.
watchtower.ico
